generator client {
  provider = "prisma-client-js"
}
datasource db {
  provider = "mysql"
  url = env("DATABASE_URL")
  relationMode = "prisma"
}

model News {
  id    Int    @id @default(autoincrement())
  title  String
  contant String
  url String
  created_at DateTime
}